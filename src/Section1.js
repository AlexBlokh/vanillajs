import { html } from "https://unpkg.com/lit-html?module";

// Define a template
const MyTemplate = (name) => html`
  <div>ASD</div>
  <p>Hello ${name}</p>
  <div>ASD</div>
`;

export default MyTemplate;
